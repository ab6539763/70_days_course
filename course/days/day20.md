# Day 20: 多模态与嵌入模型

> **零基础大模型应用开发 70 天培训课程** | 第 20/70 天 | 大模型基础理论与 Prompt 工程


——————





## 深度讲义

### 20.1 Embedding 原理

```
"猫" → [0.2, 0.8, 0.1, ...]   (1536维向量)
"狗" → [0.3, 0.7, 0.2, ...]   (语义相近，向量距离近)
"汽车" → [0.9, 0.1, 0.5, ...]  (语义不同，向量距离远)
```

### 20.2 余弦相似度

```python
import numpy as np

def cosine_similarity(a, b):
    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))

# 相似度范围: -1 到 1，越接近 1 越相似
```

### 20.3 相似问题匹配

```python
# 1. 预存问题库并向量化
# 2. 用户新问题向量化
# 3. 计算与所有预存问题的相似度
# 4. 返回最相似的问题及答案
```


## 常见错误与避坑

- ⚠️ 只看不练 — 必须动手敲代码
- ⚠️ 跳过基础直接调 API — 每天知识环环相扣
- ⚠️ 不写注释 — 企业级代码必须可读

## 课堂练习

- 📝 复习 Day 19 的核心知识点
- 📝 完成今日实操项目并提交 Git
- 📝 用 3 句话总结今天学到了什么


## 课程衔接说明



### ⬅️ 昨日回顾

**Day 19** 学习了「Function Calling / Tool Use（重点日！）」，今天的知识直接建立在昨天之上。

### 🔗 今日定位

承接 Day 19 工具调用，Embedding 是 Day 29-30 RAG 的核心。

### ➡️ 明日预告

**Day 21** 将学习「周测 + 综合练习」，今天务必打牢基础。


### 今日时间安排

| 时段 | 内容 |
|------|------|
| 09:00-12:00 上午 | 多模态模型 API 使用（图片理解、图片生成 DALL-E / 通义万相） |
| 09:00-12:00 上午 | Embedding 嵌入模型详解：文本向量化、余弦相似度计算 |
| 09:00-12:00 上午 | 动手计算句子相似度，理解语义检索的原理（RAG 的核心前置知识） |
| 14:00-17:30 下午 | 🛠️ 用 Embedding 实现一个「相似问题匹配」小工具 |
| 19:00-21:00 晚自习 | 可视化 2D 词向量散点图（可选降维） |



## 一、今日学习目标



完成今天的学习后，你将能够:

- 多模态模型 API 使用（图片理解、图片生成 DALL-E / 通义万相）
- Embedding 嵌入模型详解：文本向量化、余弦相似度计算
- 动手计算句子相似度，理解语义检索的原理（RAG 的核心前置知识）

### 核心技能点

- **Embedding**
- **余弦相似度**
- **语义检索**

### 与课程主线的关系

今天是 **第 2 阶段（大模型基础理论与 Prompt 工程）** 的第 6 天。

> 今日主题「多模态与嵌入模型」是整条 70 天学习路径中的关键一环。请对照总路线图理解今天学什么、为什么学、后面哪里会用到。



## 二、理论精讲（纳米级拆解）


### 2.1 多模态模型 API 使用（图片理解、图片生成 DALL-E / 通义万相）

#### HTTP 协议基础

| 方法 | 用途 | 大模型场景 |
|------|------|-----------|
| GET | 获取资源 | 查询模型列表 |
| POST | 提交数据 | **调用 Chat API（最常用）** |

#### 关键概念

- **URL**: 接口地址，如 `https://api.deepseek.com/v1/chat/completions`
- **Header**: 请求头，携带 `Authorization: Bearer <API_KEY>`
- **Body**: 请求体，JSON 格式，包含 `model`、`messages` 等
- **Status Code**: 200=成功, 401=密钥错误, 429=频率限制

#### 大模型 API 请求结构

```python
import requests

API_KEY = "your-api-key"
URL = "https://api.deepseek.com/v1/chat/completions"

headers = {
    "Authorization": f"Bearer {API_KEY}",
    "Content-Type": "application/json",
}

payload = {
    "model": "deepseek-chat",
    "messages": [
        {"role": "system", "content": "你是一个有帮助的助手。"},
        {"role": "user", "content": "你好！"},
    ],
    "temperature": 0.7,
}

response = requests.post(URL, headers=headers, json=payload)
result = response.json()
print(result["choices"][0]["message"]["content"])
```

> 这段代码将在今天下午的实操中完整实现。

### 2.2 Embedding 嵌入模型详解：文本向量化、余弦相似度计算

#### RAG 完整链路

```
文档 → 加载 → 分割 → 向量化 → 存储 → 检索 → 增强生成 → 回答
```

#### 核心公式（通俗版）

> 用户问题 → 转成向量 → 在知识库中找最相似的文本块 → 塞进 Prompt → 大模型生成回答

#### 关键参数

| 参数 | 作用 | 调优建议 |
|------|------|----------|
| chunk_size | 每个文本块大小 | 300-1000 字符 |
| chunk_overlap | 块之间重叠 | chunk_size 的 10-20% |
| top_k | 检索返回数量 | 3-10 |
| similarity_threshold | 相似度阈值 | 0.5-0.8 |

### 2.3 动手计算句子相似度，理解语义检索的原理（RAG 的核心前置知识）

#### RAG 完整链路

```
文档 → 加载 → 分割 → 向量化 → 存储 → 检索 → 增强生成 → 回答
```

#### 核心公式（通俗版）

> 用户问题 → 转成向量 → 在知识库中找最相似的文本块 → 塞进 Prompt → 大模型生成回答

#### 关键参数

| 参数 | 作用 | 调优建议 |
|------|------|----------|
| chunk_size | 每个文本块大小 | 300-1000 字符 |
| chunk_overlap | 块之间重叠 | chunk_size 的 10-20% |
| top_k | 检索返回数量 | 3-10 |
| similarity_threshold | 相似度阈值 | 0.5-0.8 |

## 三、下午实操预告

今日下午核心项目: **相似问题匹配工具**
- 用 Embedding 实现一个「相似问题匹配」小工具



## 下午实操：项目实战



### 项目名称

**相似问题匹配工具**

### 推荐项目目录结构（企业级标准）

```text
day20_project/
├─ src/
│  ├─ __init__.py
│  └─ main.py
├─ data/
├─ outputs/
├─ tests/
├─ requirements.txt
└─ README.md
```

### 代码骨架

```python
# ================================
# 文件名: day20_main.py
# 主题: Day 20 — 相似问题匹配工具
# ================================

"""
Day 20 实操项目入口。

学习要点:
1. 复习昨天所学
2. 完成今日「相似问题匹配工具」核心功能
3. 添加必要注释，提交 Git
"""


def main():
    """主函数 — 按今日课纲逐步实现"""
    print("Day 20: 相似问题匹配工具")
    # TODO: 按课件逐步实现
    pass


if __name__ == "__main__":
    main()
```

### 实现步骤（纳米级拆解）

1. **需求确认**: 阅读今日课纲，明确输入/输出
2. **环境准备**: 激活 venv，`pip install` 今日所需依赖
3. **核心实现**: 按上午所学知识点逐步编码
4. **自测**: 手动运行 3 个以上测试用例
5. **提交**: `git add . && git commit -m "Day 20: 相似问题匹配工具"`



## 知识小测




**Q1.** 请用自己的话解释「Embedding」是什么，并举一个与大模型开发相关的例子。

<details>
<summary>参考答案思路</summary>

- 定义: （学员填写）
- 例子: （结合 Day 17-20 所学填写）
- 后续应用: 将在 Day 27 左右用到

</details>

**Q2.** 请用自己的话解释「余弦相似度」是什么，并举一个与大模型开发相关的例子。

<details>
<summary>参考答案思路</summary>

- 定义: （学员填写）
- 例子: （结合 Day 17-20 所学填写）
- 后续应用: 将在 Day 27 左右用到

</details>

**Q3.** 请用自己的话解释「语义检索」是什么，并举一个与大模型开发相关的例子。

<details>
<summary>参考答案思路</summary>

- 定义: （学员填写）
- 例子: （结合 Day 17-20 所学填写）
- 后续应用: 将在 Day 27 左右用到

</details>


### 上机题

请不看课件，独立完成今日「实操项目」的核心功能。限时 60 分钟。



## 课后作业



### 必做

1. 完成今日实操项目「相似问题匹配工具」
2. 提交代码到 GitHub（commit message: `Day 20: 相似问题匹配工具`）
3. 在学习笔记中记录 3 个今天学到的新知识点

### 晚自习

- 可视化 2D 词向量散点图（可选降维）

### 选做

1. 阅读官方文档中与今日主题相关的章节
2. 尝试优化今日代码（更优雅的错误处理、更清晰的注释）


——————

**第 20/70 天 · 零基础大模型应用开发 70 天培训课程**

> 坚持每一天，70 天后你将成为一名合格的大模型应用开发者。
