# Day 1 课堂笔记精华（可打印 A4）

## 今日一句话

**程序 = 数据 + 操作；今天学会用 Python 存数据、和用户对话、把结果漂亮地打印出来。**

---

## 知识脑图（手写填空版）

```
大模型职业路径
    ├── 应用开发（本课程主对标）
    ├── 产品经理 / 实施
    └── 算法 / 训练推理

Python 环境
    ├── python --version  ≥ 3.10
    ├── VS Code + Python 扩展
    └── pip 国内镜像

基本类型
    ├── int    _______
    ├── float  _______
    ├── str    _______
    └── bool   _______

输入输出
    ├── input() → 永远是 _______
    └── print(f"...{变量}...")  → f-string

今日项目
    采集 → 校验 → dict → 渲染 → 可选写文件
```

---

## 必背命令

```bash
python --version
python hello.py
python -m src.main
git add .
git commit -m "Day1: 描述"
git push
```

---

## 与后续课程衔接

| 今日 | 日后用途 |
|------|----------|
| dict | JSON / API 响应 |
| str | Prompt 模板 |
| input | CLI → Web 表单 |
| 函数拆分 | FastAPI 路由、RAG 管道 |

---

## 今日自查 5 题（答案见作业文档）

1. `input("年龄")` 用户输入 `18`，类型是？
2. f-string `f"{3.14159:.2f}"` 输出？
3. Git 的 commit 解决什么问题？
4. 为什么年龄要 `int()` 转换？
5. 名片项目为什么要拆 `validators.py`？

---

**Day 2 预告**：运算符、字符串 `strip/split/replace`、文本清洗小工具
